<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><head><meta charset="UTF-8"></head><body><p>{%- set form_state = form_state | default(settings['form-state']) | default('default') -%}
{%- set classes = ['e-con', 'e-atomic-element', base_styles.base, 'form-state-' ~ form_state] | merge(settings.classes | default([])) | join(' ') -%}
</p><form class="{{ classes }} {{ editor_classes | default('') }}" data-id="{{ id }}" data-element_type="{{ type }}" data-e-type="{{ type }}" data-interaction-id="{{ interaction_id }}" data-interactions="{{ interactions | json_encode | e('html_attr') }}" x-data="eForm{{ id }}" x-on:submit="submit" if settings is not empty aria-label="{{ settings['form-name'] | e('html_attr') }}" data-form-name="{{ settings['form-name'] | e('html_attr') }}" endif webmcp_tool_name toolname="{{ webmcp_tool_name | e('html_attr') }}" tooldescription="{{ webmcp_tool_description | e('html_attr') }}" toolautosubmit id="{{ settings['_cssid'] | e('html_attr') }}" editor_attributes default raw>
    <!-- elementor-children-placeholder -->
</form>
</body></html>
